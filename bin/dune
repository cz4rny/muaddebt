(executable
 (public_name muaddebt)
 (name main)
 (preprocess
  (pps ppx_deriving.show))
 (libraries muaddebt))

(env
 (static
  (link_flags
   (:standard -ccopt -static -ccopt -no-pie -cclib -static))))
